import { ConfigurationOptions } from 'i18n';

const configs: ConfigurationOptions = {
	locales: [ 'en' ],
	defaultLocale: 'en',
	directory: 'src/infrastructure/locales',
	objectNotation: true
};

export default configs;
