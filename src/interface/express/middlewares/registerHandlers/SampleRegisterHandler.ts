import { ExpressRegisterHandlerType } from 'confork-core';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const handler: ExpressRegisterHandlerType = (fields) => (req, res, next) => {
	//
};

export default handler;
